# Nibras
On-device security scanner for AI-generated code, merging two audience framings into one engine. Core: scans code for exposed API keys/secrets, injection risks, missing input validation, hallucinated/non-existent package imports, common code smells — runs fully on-device via QVAC, nothing touches a server.
